const RootLoading = () => {
    return (
        <div className="fixed inset-0 bg-slate-200 dark:bg-slate-800 flex justify-center items-center">
            <h1 className="text-center">Loading...</h1>
        </div>
    );
};

export default RootLoading;
